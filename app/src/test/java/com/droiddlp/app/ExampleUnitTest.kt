package com.droiddlp.app

import kotlin.test.Test
import kotlin.test.assertEquals

/** Smoke test that establishes the unit-test harness for the project. */
class ExampleUnitTest {
    @Test
    fun arithmetic_sanity() {
        assertEquals(4, 2 + 2)
    }
}
